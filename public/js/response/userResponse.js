import { showAlert } from "../alerts/alertResponse.js"

class response{

    constructor(response){
        this.status = response.status;
        this.message = response.message;
    }

    login(){

        if(this.status >= 200 && this.status < 300){



        }


    }
}